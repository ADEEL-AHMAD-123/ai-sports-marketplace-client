// DEPRECATED — AI Logs admin page is no longer surfaced.
// AI logs still get auto-pruned by the daily 3AM cron via aiLogExpiresAt TTL;
// admins didn't need a manual viewer. References removed from App.jsx + AdminLayout.jsx.
export default function _Deprecated() { return null; }
